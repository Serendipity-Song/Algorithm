

#include <iostream>
#include <string>
using namespace std;


int main()
{
    int n;
    cin >> n; //단어의 갯수

    int cnt = 0;

    for (int i = 0; i < n; i++) {
        char word[100];
        char word;
        cin >> word;
        //같은 문자들이 붙어있다
            //해당 문자의 총 갯수가 연속수와 같다
            if (word[i].search== rfind(s)- find(s ) {

                cnt++;
            }
    }

    cout << cnt << endl;

    return 0;
}

